/**
 * Document IDs created by `scripts/seed-firestore.mjs` — only used to pre-render
 * dynamic routes when building with `output: 'export'`.
 */
export const STATIC_EXPORT_PROPOSAL_IDS = ["prop-seed-1"] as const;
export const STATIC_EXPORT_PROJECT_IDS = ["proj-seed-1"] as const;
