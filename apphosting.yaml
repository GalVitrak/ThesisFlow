# Firebase App Hosting — https://console.firebase.google.com/project/_/apphosting
# Connect the repo in Console; set NEXT_PUBLIC_* in Console → Environment (overrides this file).
runConfig:
  minInstances: 0
# env:
#   - variable: NEXT_PUBLIC_USE_FIREBASE
#     value: "true"
#     availability: [BUILD, RUNTIME]
#   - variable: NEXT_PUBLIC_FIREBASE_API_KEY
#     value: "…"
#     availability: [BUILD, RUNTIME]
#   (see .env.example for the rest)
